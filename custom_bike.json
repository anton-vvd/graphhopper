{
  "priority": [
    { "if": "road_class == CYCLEWAY", "multiply_by": 1.0 },
    { "if": "road_class == RESIDENTIAL", "multiply_by": 0.9 },
    { "if": "road_class == TERTIARY", "multiply_by": 0.8 },
    { "if": "road_class == SECONDARY", "multiply_by": 0.7 },
    { "if": "road_class == PRIMARY", "multiply_by": 0.6 },
    { "if": "road_class == TRUNK", "multiply_by": 0.3 },
    { "if": "road_class == MOTORWAY", "multiply_by": 0.0 }
  ],
  "speed": [
    { "if": "road_class == CYCLEWAY", "limit_to": 30 },
    { "if": "road_class == RESIDENTIAL", "limit_to": 25 },
    { "if": "road_class == TERTIARY", "limit_to": 20 },
    { "if": "road_class == SECONDARY", "limit_to": 20 },
    { "if": "road_class == PRIMARY", "limit_to": 18 },
    { "if": "surface == GRAVEL", "multiply_by": 0.7 },
    { "if": "surface == DIRT", "multiply_by": 0.5 },
    { "if": "surface == SAND", "multiply_by": 0.3 }
  ]
} 